query getSingleUser($id: ID!) {
  user(id: $id) {
    id
    username
    email
    stripeID
    programs {
      id
      name
      description
      image {
        id
        url
      }
    }
  }
}