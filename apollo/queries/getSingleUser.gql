query getSingleUser($id: ID!) {
  user(id: $id) {
    id
    username
    email
    stripeID
    subscription
    paymentMethod
    programs {
      id
      name
      description
      image {
        url
      }
    }
  }
}